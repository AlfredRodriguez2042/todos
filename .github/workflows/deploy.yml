name: Manual trigger

on:
  workflow_dispatch:
  release:
    types: [created]
    inputs:
      name:
        description: "Who to greet"
        default: "World"
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: saludo
        run: echo "hello"
        
